name: Deploy to GitHub Pages

on:
  push:
    paths:
      - '.github/workflows/doploy-pages.yml'
      - 'core/_updater.sh'
      - 'docs/**'
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Install UV
        uses: astral-sh/setup-uv@v5
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: "docs/pyproject.toml"
        
      - name: Install Dependencies
        run: |
          echo "Installing dependencies..."
          ( cd docs && uv sync )
          echo "Dependencies installed successfully."

      - name: Compile docs
        run: |
          echo "Compiling documentation..."

          uv tool run mkdocs build --config-file docs/mkdocs.yml --site-dir build

          echo "Documentation compiled successfully."

      - name: Create .nojekyll & README file
        run: |
          echo "" > docs/build/.nojekyll
          echo '<h1 align="center">THIS BRANCH IS AUTO-GENERATED</h1>' > docs/build/README.md

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: docs/build
          branch: docs
